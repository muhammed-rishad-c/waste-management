<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gift Cards</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #F4F9F7; /* Light greenish background */
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            position: relative; /* Ensure the absolute positioning of the button works */
        }

        h1 {
            color: #27AE60; /* Green color */
            font-family: 'Poppins', sans-serif;
            font-size: 2rem;
            margin: 20px 0;
            text-align: center;
        }

        /* Home Button */
        .home-btn {
            font-family: 'Poppins', sans-serif;
            text-decoration: none;
            color: #27AE60;
            font-weight: 600;
            padding: 10px 20px;
            border: 2px solid #27AE60;
            border-radius: 8px;
            display: inline-flex;
            align-items: center;
            background-color: #F4F9F7;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            position: absolute; /* Positioning it at the top-left */
            top: 20px;  /* 20px from the top */
            left: 20px; /* 20px from the left */
        }

        .home-btn i {
            margin-right: 8px;
        }

        .home-btn:hover {
            background-color: #27AE60;
            color: #FFF;
            transform: translateY(-3px);
        }

        .btn {
            background-color: #fff; /* Default white background */
            color: #27AE60; /* Green text */
            padding: 14px 25px;  /* Increased padding */
            border-radius: 10px;  /* Slightly larger border radius */
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            transition: all 0.3s ease;
            text-align: center;
            margin-top: 20px;
            font-size: 1.1rem;  /* Slightly larger font size */
            border: 2px solid #27AE60; /* Green border */
        }

        .btn:hover {
            background-color: #27AE60; /* Green background on hover */
            color: #fff; /* White text on hover */
            transform: translateY(-3px);
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .card {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            width: 280px;
            padding: 20px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card img {
            border-radius: 12px;
            width: 100%;
            height: 200px;
            object-fit: cover;
            margin-bottom: 15px;
        }

        .card h2 {
            color: #2C3E50;
            font-size: 1.4rem;
            margin: 10px 0;
        }

        .card p {
            color: #7F8C8D;
            font-size: 1rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .card-container {
                padding: 0 10px;
            }

            .card {
                width: 100%;
                max-width: 320px;
            }

            h1 {
                font-size: 1.8rem;
            }

            .btn {
                font-size: 1rem;
                padding: 12px 22px;  /* Adjusted padding for mobile */
            }
        }
    </style>
</head>
<body>

    <!-- Home Button -->
    <a href="/user/home" class="home-btn"><i class="fas fa-arrow-left"></i> Back to Home</a>

    <h1>Gift Cards Available</h1>

    <!-- Gift Cards Container -->
    <div class="card-container">
        <% giftCards.forEach(giftCard => { %>
            <div class="card">
                <img src="/uploads/<%= giftCard.photo %>" alt="<%= giftCard.name %>">
                <h2><%= giftCard.name %></h2>
                <p>Points Needed: <%= giftCard.point %></p>
                <a href="/user/redeem/<%= giftCard.id %>" class="btn">Redeem Reward</a>
            </div>
        <% }) %>
    </div>

</body>
</html>
